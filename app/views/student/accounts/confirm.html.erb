<%= @title = "アカウントの更新(確認)" %>
<h1><%= @title %></h1>

<%= form_with model: @student_form, scope: "form",
  url: :student_account do |f| %>
  <%= render "form", f: f %>
  <%= f.submit "更新" %>
  <%= f.submit "訂正", name: "correct" %>
<% end %>