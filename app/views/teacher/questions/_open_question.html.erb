<% unless open_question.closed %>
    <%= open_question.book.book_name %>
    <%= "(#{open_question.subject})"%>
    <%= open_question.title  %>
    <%= open_question.body %>
    <%= open_question.created_at.strftime('%Y/%m/%d %H:%M') %>
    <%= link_to "詳細", [ :teacher, open_question ] %>
  <% end %>