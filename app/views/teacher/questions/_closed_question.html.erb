<% if closed_question.closed %>
    <%= closed_question.book.book_name %>
    <%= "(#{closed_question.subject})"%>
    <%= closed_question.title  %>
    <%= closed_question.body %>
    <%= closed_question.created_at.strftime('%Y/%m/%d %H:%M') %>
    <%= link_to "詳細", [ :teacher, closed_question ] %>
  <% end %>